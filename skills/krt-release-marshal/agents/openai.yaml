interface:
  display_name: "krt-release-marshal"
  short_description: "Drive commit, rebase, Jira, PR, and backlink flow"
  default_prompt: "Use krt-release-marshal to coordinate commit planning, safe rebasing, Jira handling, GitHub PR creation, and bidirectional PR/Jira links."
